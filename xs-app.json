{
  "welcomeFile": "/index.html",
  "authenticationMethod": "route",
  "routes": [
    {
      "comment": "All ReconcileServices calls (reconciliation + pick info) via the CloudWMReconcilliation destination. CSRF is handled by CAP itself, so the approuter does not double-guard it.",
      "source": "^/ReconcileServices/(.*)$",
      "target": "/ReconcileServices/$1",
      "destination": "CloudWMReconcilliation",
      "authenticationType": "xsuaa",
      "csrfProtection": false
    },
    {
      "comment": "Static app content served from the HTML5 Application Repository.",
      "source": "^/(.*)$",
      "target": "/$1",
      "service": "html5-apps-repo-rt",
      "authenticationType": "xsuaa"
    }
  ]
}
