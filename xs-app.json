{
  "welcomeFile": "/index.html",
  "authenticationMethod": "route",
  "routes": [
    {
      "comment": "All CAP OData V4 services (ECC fetch, HANA fetch, HANA create) via the CloudWM-CAP destination. CSRF is handled by CAP itself, so the approuter does not double-guard it.",
      "source": "^/odata/v4/(.*)$",
      "target": "/odata/v4/$1",
      "destination": "CloudWM-CAP",
      "authenticationType": "xsuaa",
      "csrfProtection": false
    },
    {
      "comment": "Static app content served from the HTML5 Application Repository.",
      "source": "^/(.*)$",
      "target": "/$1",
      "service": "html5-apps-repo-rt",
      "authenticationType": "xsuaa"
    }
  ]
}
